# TypeScript Decision - SAM MVP

## 1. Titel

TypeScript Decision - SAM MVP

## 2. Status

`APPROVED`

## 3. Besluit

Gebruik `TypeScript` als primaire taalrichting voor `SAM MVP Release 1`.

## 4. Reden

* past goed bij `React/Vite` cockpit
* past goed bij `Node.js` API
* helpt fouten eerder zichtbaar maken
* ondersteunt gedeelde contracts/types tussen `web`, `api` en `shared`
* maakt Codex-output beter controleerbaar
* past bij `Prisma ORM` en `PostgreSQL`-richting

## 5. Grenzen

* geen `TypeScript`-installatie in deze stap
* geen `tsconfig` in deze stap
* geen source files in deze stap
* geen app-code
* geen oude `SAM V2`-code overnemen

## 6. Vervolgactie

Na dit besluit mag in een aparte opdracht minimale `TypeScript`-configuratie worden voorbereid.
